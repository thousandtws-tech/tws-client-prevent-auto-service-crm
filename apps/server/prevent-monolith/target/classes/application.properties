spring.application.name=${SPRING_APPLICATION_NAME:prevent-monolith}
spring.codec.max-in-memory-size=${SPRING_CODEC_MAX_IN_MEMORY_SIZE:10MB}
spring.jackson.default-property-inclusion=non_null

spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true
spring.flyway.validate-on-migrate=true
spring.flyway.locations=classpath:db/migration
spring.flyway.url=jdbc:postgresql://${DB_HOST:ep-shiny-fog-ad6tt0k0-pooler.c-2.us-east-1.aws.neon.tech}:${DB_PORT:5432}/${DB_NAME:prevent-database}?sslmode=${DB_SSL_MODE:require}&channelBinding=${DB_CHANNEL_BINDING:require}
spring.flyway.user=${DB_USERNAME:prevent-user}
spring.flyway.password=${DB_PASSWORD:npg_xi5PGcyZYo1u}

spring.r2dbc.url=r2dbc:postgresql://${DB_HOST:ep-shiny-fog-ad6tt0k0-pooler.c-2.us-east-1.aws.neon.tech}:${DB_PORT:5432}/${DB_NAME:prevent-database}?sslMode=${DB_SSL_MODE:require}
spring.r2dbc.username=${DB_USERNAME:prevent-user}
spring.r2dbc.password=${DB_PASSWORD:npg_xi5PGcyZYo1u}
spring.r2dbc.pool.enabled=true
spring.r2dbc.pool.initial-size=${DB_POOL_INITIAL_SIZE:5}
spring.r2dbc.pool.max-size=${DB_POOL_MAX_SIZE:20}
spring.r2dbc.pool.max-idle-time=${DB_POOL_MAX_IDLE_TIME:30m}

server.port=${PORT:${SERVER_PORT:9090}}
server.shutdown=graceful

security.jwt.secret=${AUTH_JWT_SECRET:prevent-auth-development-secret-key-2026-change-me}
security.jwt.access-token-ttl=${AUTH_JWT_ACCESS_TOKEN_TTL:15m}
security.jwt.refresh-token-ttl=${AUTH_JWT_REFRESH_TOKEN_TTL:30d}

media.cloudinary.enabled=${CLOUDINARY_ENABLED:false}
media.cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME:dao3brh15}
media.cloudinary.api-key=${CLOUDINARY_API_KEY:289111322439863}
media.cloudinary.api-secret=${CLOUDINARY_API_SECRET:OKlmhKNKzKlJIfNC4huM0VBP6hM}
media.cloudinary.base-folder=${CLOUDINARY_BASE_FOLDER:prevent-auto-mecanica}
media.cloudinary.max-file-size=${CLOUDINARY_MAX_FILE_SIZE:5MB}

backup.storage.base-path=${BACKUP_STORAGE_BASE_PATH:./data/backups}
backup.default-timezone=${BACKUP_DEFAULT_TIMEZONE:America/Sao_Paulo}
backup.default-schedule-time=${BACKUP_DEFAULT_SCHEDULE_TIME:02:00}
backup.default-schedule-days=${BACKUP_DEFAULT_SC HEDULE_DAYS:MONDAY,TUESDAY,WEDNESDAY,THURSDAY,FRIDAY}
backup.scheduler.fixed-delay-ms=${BACKUP_SCHEDULER_FIXED_DELAY_MS:300000}
backup.scheduler.initial-delay-ms=${BACKUP_SCHEDULER_INITIAL_DELAY_MS:60000}

service-orders.public-signature-base-url=${SERVICE_ORDERS_PUBLIC_SIGNATURE_BASE_URL:https://projeto-prevent-auto-mecanica-clien.vercel.app/assinatura-os}

cors.allowed-origin-patterns=${CORS_ALLOWED_ORIGIN_PATTERNS:http://localhost:5173,http://127.0.0.1:5173,https://projeto-prevent-auto-mecanica-clien.vercel.app,https://*.vercel.app}

spring.mail.host=${SPRING_MAIL_HOST:smtp.gmail.com}
spring.mail.port=${SPRING_MAIL_PORT:587}
spring.mail.username=${SPRING_MAIL_USERNAME:vinicius.nextjs@gmail.com}
spring.mail.password=${SPRING_MAIL_PASSWORD:pbyq kxmh mxsb bruh}
spring.mail.properties.mail.smtp.auth=${SPRING_MAIL_SMTP_AUTH:true}
spring.mail.properties.mail.smtp.starttls.enable=${SPRING_MAIL_SMTP_STARTTLS_ENABLE:true}
spring.mail.properties.mail.smtp.starttls.required=${SPRING_MAIL_SMTP_STARTTLS_REQUIRED:false}

spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.mode=HTML
spring.thymeleaf.cache=${SPRING_THYMELEAF_CACHE:false}

auth.email-verification.mail-from=${AUTH_EMAIL_VERIFICATION_MAIL_FROM:no-reply@prevent.local}
auth.email-verification.verify-url-base=${AUTH_EMAIL_VERIFICATION_VERIFY_URL_BASE:http://localhost:9090/auth/verify-email}
auth.email-verification.redirect-url=${AUTH_EMAIL_VERIFICATION_REDIRECT_URL:http://localhost:5173/verificacao-email}
auth.email-verification.token-ttl=${AUTH_EMAIL_VERIFICATION_TOKEN_TTL:24h}

management.endpoints.web.exposure.include=${MANAGEMENT_ENDPOINTS_WEB_EXPOSURE_INCLUDE:health,info}
management.endpoint.health.probes.enabled=${MANAGEMENT_ENDPOINT_HEALTH_PROBES_ENABLED:true}
management.health.livenessstate.enabled=${MANAGEMENT_HEALTH_LIVENESSSTATE_ENABLED:true}
management.health.readinessstate.enabled=${MANAGEMENT_HEALTH_READINESSSTATE_ENABLED:true}
management.endpoint.health.show-details=${MANAGEMENT_ENDPOINT_HEALTH_SHOW_DETAILS:never}

springdoc.api-docs.groups.enabled=true
springdoc.swagger-ui.path=${SPRINGDOC_SWAGGER_UI_PATH:/swagger-ui.html}
springdoc.swagger-ui.disable-swagger-default-url=true
